[pytest]
addopts = -ra
testpaths = tests
pythonpath = src
markers =
    integration: tests that hit the real MySQL database and FastAPI stack
    e2e: end-to-end tests that exercise API flows and may require live services
