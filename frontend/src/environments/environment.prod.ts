export const environment = {
  production: true,
  // API URL can be configured via environment variable during build
  // If not set, will fall back to hostname-based logic
  API_URL: '${API_URL}',
};